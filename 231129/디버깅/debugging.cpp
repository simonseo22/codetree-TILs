// 최소한의 유실선으로 버그를 없애는 프로그램
// 유실선은 선이 겹쳐서 추가는 불가능
// *입력
// 1)고객의수 n(2~10), 유실선 m(0~(n-1)*h), 취약지점 h(1~30)  공백을 사이에 두고
// 2~m+1) 유실선의 정보, 취약지점과 메모리 유실이 일어난 지점이 정수로 공백을 사이에 두고
// 취약지점(가로) a, 메모리 유실 b(세로)라면 a취약점에서 b에서 b+1번째로 유실

#include <iostream>

int main() {
    // 여기에 코드를 작성해주세요.
    return 0;
}